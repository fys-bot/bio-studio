# 交付前 QA 核查（2026-09-09）

本轮从研究员首次登录、任务切换、文件上传/预览、RAG 问答、计划 SSE 到异常恢复逐项核查。以下条目均已在本地代码中处理；运行态测试数据不进入 Git。

| # | 发现 | 修复 | 验证 |
| --- | --- | --- | --- |
| 1 | Next 开发缓存可能导致任务动态路由 `installChunk` 崩溃 | 启动器增加工作区归属检测与缓存指纹清理 | 任务路由返回 200 |
| 2 | 端口探测没有超时，`npm run services` 表现为无响应 | TCP 探测增加 900ms 超时 | 端口冲突立即输出状态 |
| 3 | 旧 Worker 仅健康检查通过，但缺少当前接口实现 | 加入 OpenAPI 和契约版本校验 | 自动跳过旧 Worker |
| 4 | Python 依赖失败时没有可读启动阶段 | 服务启动输出依赖检查、Worker、Qdrant 和退出原因 | `npm run services` 输出明确阶段 |
| 5 | 8001 遗留进程不可终止会阻断服务 | 启动器探测 8002-8004 并复用兼容 Worker | 当前复用 `8002` Worker |
| 6 | PDF iframe 不会携带 Bearer，预览返回 401 | 授权请求读取字节并生成短生命周期 Blob URL | 授权原件 200，未授权 401 |
| 7 | 受限图片与 PDF 的原件访问行为不一致 | 统一受保护二进制预览组件 | 两类文件走相同授权路径 |
| 8 | 正常科研 PDF 的复杂矢量页被 8MB 阈值拒绝 | 改为 32MB 安全上限，复杂页只记录提示 | 23 页 PDF 成功解析并索引 |
| 9 | 文件服务失败后文件中心是空白大区域 | 顶层错误条、有限自动重连和文件管理空状态 | Worker 恢复后自动重读目录 |
| 10 | 能力中心无结果只显示一句文字 | 替换为带创建入口的领域空状态 | 无筛选结果仍可继续操作 |
| 11 | 上传限制与界面/接口文档不一致 | 前端、BFF、Worker、README 和 API 规范统一为 300MB | 统一显示和校验口径 |
| 12 | 演示 Trace 在无文件时可被包装为模型回答 | 无绑定材料时拒绝 `includeAnswer=true` | 返回可读 409，不伪装真实答案 |
| 13 | 对话只传问题，任务/历史上下文存在保存竞态 | 请求显式携带当前对话，服务端合并任务、澄清、计划和最近历史 | 真实 LLM 问答返回来源引用 |
| 14 | 首页静态话术容易被误认为 LLM 输出 | 改为“工作区提示”，无材料时明确说明边界 | 页面不再伪称模型结论 |
| 15 | 模型请求失败只留在局部消息区域 | 全局 API 错误层固定最高层；局部保留重试入口 | 错误始终在页面顶层可见 |
| 16 | 任务选中态视觉弱 | 加强边线、底色、左侧强调线与文字对比 | 当前任务可一眼定位 |
| 17 | 默认演示任务没有删除入口而无解释 | 显示锁定图标和工具提示；避免删除唯一演示主线 | 受保护原因可见 |
| 18 | 切换亮点示例时右侧检查器闪现旧内容 | 路由切换期间不提前打开检查器 | 新任务水合完成后再由用户打开 |
| 19 | 引导每次登录自动出现，且所有账号共用状态 | 改为账号级“已看过/不再提醒”键 | 一个账号关闭不影响其他账号 |
| 20 | 移动端首步引导找不到亮点示例 | 在目标区放入三个可操作的示例图标 | 引导首步可准确定位 |
| 21 | 移动端引导卡片偏大，能力信息挤压 | 移动端改为可滚动、单列能力信息和安全区定位 | 小屏不遮挡底部导航 |
| 22 | 计划 SSE 在 Python 网关超时后直接失败 | 保留 Worker 失败事件，回退同一配置的 Next LLM 适配器 | SSE 完整返回 `plan.completed` 与 `gpt-5.5` 计划 |
| 23 | 真实文件流程缺少端到端证据 | 上传 DOCX/PDF，解析、索引、原件预览、RAG 与 LLM 已实测 | 两文件均为 `indexed` |
| 24 | 运行数据和临时 Python 环境可能误提交 | 忽略运行目录与 `.venv-repair` | `data/catalog-state.json` 未纳入提交 |

## 本轮关键实测

- TypeScript：`npm run typecheck` 通过。
- 解析单测：6/6 通过（DOCX、XLSX、原生 PDF、扫描 PDF、空 PDF、损坏 Office）。
- 真实文档：`s41467-026-74300-9.pdf` 与 `20150507140548_572029_23573.docx` 均完成解析和向量索引。
- RAG：两份文件均被召回；真实模型 `gpt-5.5` 返回了带引用的中文回答。
- SSE：已产生逐步事件并完成计划写入，模型标识为 `gpt-5.5`。

## 环境备注

本机 `node_modules` 和原始 `.venv` 出现 macOS 按需文件读取 `ETIMEDOUT`，会导致生产构建或 Python import 卡住。启动器已对 Worker 给出明确诊断并优先使用健康的本地运行时；类型检查、解析单测、真实文件/RAG/SSE 验证均已完成。生产构建需要在依赖文件可本地完整读取的环境中再次执行。
