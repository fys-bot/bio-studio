# 变更记录

本文件记录 BioFlow Studio 每次可交付改动，提交代码时同步更新。

## 2026-09-07（P1-2 CSV / TSV 真实解析）

- 新增受鉴权和同源保护的 `/api/files/profile` 接口，CSV / TSV 原始内容仅在服务端处理，前端只接收结构摘要。
- 新增独立表格分析模块，支持 CSV 引号与 TSV 解析、缺失值统计、字段类型推断、Count 矩阵 / 样本元数据识别和实验分组候选评分。
- 增加 5 MB 文件、512 列和 100,000 行安全边界；不支持的格式、空文件与异常结构返回明确中文错误。
- 文件结构摘要持久化到任务状态；刷新可恢复，同名文件采用替换语义，避免侧栏和文件中心出现重复项目。
- 上传弹窗新增样本、字段、缺失值、字段类型与智能体建议可视化，并可将识别出的 `condition` 直接映射为“处理组 vs 对照组”澄清答案。
- 新增可直接用于面试演示的 `demo-data/sample_metadata.tsv` 示例文件。

### 浏览器与工程验收

- 浏览器真实选择示例 TSV 后，页面正确显示 6 个样本、4 个字段、1 个缺失值与 `condition` 分组建议；应用建议、刷新恢复、文件列表去重均通过。
- smoke 新增文件接口未鉴权、跨域拒绝、TSV 元数据、CSV Count 矩阵、不支持格式和摘要持久化断言，既有 Agent 成功 / 失败 / 取消链路仍全部通过。
- `npm run format:check`、`npm run typecheck`、隔离生产构建和 `git diff --check` 全部通过。

## 2026-09-07（P2-4 格式化与代码质量门禁）

- 引入 Prettier 3，新增统一配置、忽略规则以及 `format`、`format:check` 命令，覆盖前端组件、API Route、领域层、脚本、CSS 和根配置文件。
- 完成全项目首轮格式化，原先压缩在长行中的页面与样式恢复为可审查结构，降低面试代码 Review 的阅读成本。
- 清理业务代码中的单字母变量和残留 `any`，为运行时 Store 增加明确领域类型，并对端口管理等关键工程逻辑补充中文注释。
- 修复开发启动器端口回收竞态：先发送 `SIGTERM` 并等待套接字释放，旧进程无响应时升级为 `SIGKILL`，确认释放后才启动 Next.js；默认端口保持 3000。

### 浏览器与工程验收

- 端口接管实测通过：在 3020 建立临时占用后执行启动器，旧 PID 被释放，Next.js 成功进入 Ready，不再触发 `EADDRINUSE`。
- 在 3019 隔离开发服务完成完整接口 smoke，鉴权、配置、SSE、失败/重试、成功、取消和结果产物全部通过。
- 浏览器真实点击通过：能力中心、空白遮罩关闭、文件入口、结果空态、代码面板、3D 面板、暂停、复位和 Mol* 提示均正常，页面无白屏。
- `npm run format:check`、`npm run typecheck`、隔离生产构建和 `git diff --check` 全部通过。

## 2026-09-07（P2-3 AI 代码流式输出）

- 新增 `code.delta` 与 `code.completed` SSE 生命周期，运行与局部重试都会产生可观察的代码增量事件。
- 新增代码产物组件，包含行号、无 HTML 注入的 Python 语法高亮、增量光标、自动滚动、生成进度与中文阶段状态。
- 未运行时显示代码空态；生成后支持复制、下载 `.py`、重新生成，代码版本随 Artifact 从草稿切换为 `v1.0.0`。
- 新增代码血缘，展示设计公式、统计方法和结果绑定，并支持定位 DESeq2 节点与打开技能包证据。
- 移除页面固定 1.8 秒结束轮询的计时器，改为仅在任务进入成功、失败或取消终态时停止同步。

### 浏览器验收

- 在成功链路点击“运行并生成代码”，真实观察 `import pandas` 到完整分析脚本逐字符增加，状态由“正在流式生成”切换为“已验证并绑定结果”。
- 复制代码、下载 `.py` 均出现成功反馈；方法证据弹窗显示“已审批计划、设计矩阵和 DESeq2 技能包共同生成”。
- smoke 增加 `code.delta` 和 `code.completed` 事件断言；类型检查、生产构建与 Git diff 纳入阶段门禁。

## 2026-09-07（P2-2 三维结构查看器）

- 将原有 CSS 结构符号替换为 Canvas 三维坐标投影，主链按深度着色并标出功能残基。
- 新增拖拽旋转、滚轮缩放、自动旋转暂停/恢复、视角复位和残基拾取；选中残基展示氨基酸、编号与 pLDDT。
- 新增 AlphaFold 结构目录，将 E2F1、CCNE2、CDK1、GADD45A、MKI67 映射到明确 accession 与模型置信度。
- 候选基因选择提升为检查器共享状态，结果面板选中的基因可直接驱动结构模型、统计摘要和跨产物关联说明。
- 三维能力按“结构模型 / Canvas 渲染适配器 / 业务面板”三层拆分，后续接入 Mol* 或 3Dmol.js 不需要改动结果业务层。

### 浏览器验收

- 在结果面板选择 CDK1 后切换 3D，显示 `AF-P06493-F1`、95.7 pLDDT、FDR 0.0028 和 log₂FC +2.07。
- 真实点击暂停旋转、复位、拖拽结构和残基点；结构视角发生变化，拾取 LEU 123 后显示 pLDDT 98 与上下文关联反馈。
- “在 Mol* 中打开完整结构”入口反馈正常；类型检查、生产构建、完整接口 smoke 与 Git diff 纳入阶段门禁。

## 2026-09-07（P2-1 结果交互与血缘追溯）

- 新增结果领域组件目录，将火山图、Artifact 血缘和结果编排从研究工具检查器中拆分。
- 候选基因、统计摘要、可视化坐标和五段结果血缘改由服务端 Artifact 返回；未运行时展示真实空态，不再提前呈现固定结果。
- 火山图增加显著性阈值、上调/下调图例、候选点 hover / 聚焦 / 点击详情；候选基因列表与图表双向共享选中状态。
- 参数快照弹层通过 Portal 挂载到页面根节点，支持遮罩关闭，修复中屏右侧抽屉内弹层被 transform 裁切的问题。
- 结果血缘支持逐步展开、定位工作流节点和打开文献证据详情；smoke 增加候选基因、统计摘要和完整血缘断言。

### 浏览器验收

- 在隔离开发实例完整走通“澄清 → 计划 → 配置成功链路 → 审批 → 运行 → 结果”，任务达到已完成 100%。
- 真实点击 E2F1 后，火山图显示上调、FDR 0.0004 和 log₂FC +2.84；点击血缘中的 DESeq2 可定位证据面板。
- 参数快照可完整居中覆盖工作台，点击遮罩关闭通过；文献证据弹窗显示 PMID 25516281。
- 空 Artifact 状态、TypeScript、生产构建、完整接口 smoke 和 Git diff 均纳入阶段门禁。

## 2026-09-07（P2-1 结果 Artifact 增强）

- 为 Artifact 增加版本、生成时间、来源节点和参数快照领域类型，结果数据不再只是文件名列表。
- 结果面板新增 Artifact 元数据、候选基因 Top 5、FDR、效应量与上下调方向展示。
- 新增 Markdown 科研报告预览和下载，报告包含研究目标、统计模型、阈值、证据绑定与结果血缘。
- 抽取服务端 `completeArtifacts` 公共完成逻辑，修复“全成功链路只完成任务但不写入产物”的真实业务缺口；成功直达和失败重试现在生成一致的 3 个 Artifact。

### 浏览器验收

- 在 3015 开发服务构造成功运行，真实查看 `v1.0.0`、中文生成时间、DESeq2 来源、候选基因列表和报告元数据。
- 点击“预览报告”可展开 Markdown；点击“下载 .md”出现“Markdown 分析报告已下载”反馈。
- `npm run smoke -- http://127.0.0.1:3015` 通过，覆盖失败/重试、成功直达、SSE、鉴权和取消分支。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。

## 2026-09-06（P1-4 对话面板组件化）

- 新增 `components/ConversationPanel.tsx`，统一承载用户消息、智能体回复、失败提示和科研问题输入区。
- 对话组件通过明确 Props 接收任务状态和业务回调，页面只负责消息状态、模式状态、弹窗和 Inspector 路由编排。
- 保留发送、Enter 快捷发送、空消息校验、Agent 模式切换、添加项目文件、失败原因和生成代码入口，确保组件拆分不改变演示行为。
- 使用 `messageIndex`、`replyIndex` 等语义化命名，并补充组件职责注释，降低面试代码 Review 成本。

### 浏览器验收

- 真实发送科研问题后，消息和标准模式智能体回复按时间线追加。
- 切换为严谨模式后 Toast 正常出现，使用 Enter 发送后回复内容带有当前模式。
- 添加项目文件弹窗可打开，按 Esc 可关闭；失败原因和生成代码入口仍由页面回调驱动。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。

## 2026-09-06（P1-5 错误码与加载状态治理）

- `lib/api-client.ts` 新增 `ApiClientError`，统一 401、403、404、校验错误、服务端错误和网络错误编码，并转换为中文用户提示。
- 对幂等 GET 请求增加有限重试和递增等待；POST 状态变更请求默认不自动重试，避免重复创建运行或重复提交实验状态。
- 澄清提交、计划审批、运行取消、节点重试增加操作锁和中文进行中状态，防止快速连续点击造成重复请求。
- Inspector 的代码运行与失败节点重试按钮在进行中时禁用，配置保存沿用既有保存锁。

### 浏览器验收

- 真实取消工作流后状态更新为“已取消”，并出现“运行已取消”反馈。
- 页面刷新后 API 客户端仍能恢复任务、配置和 Inspector 状态；构建无白屏。
- 在独立开发端口 3015 执行 `npm run smoke -- http://127.0.0.1:3015` 通过，覆盖鉴权、错误澄清、跨域保护、SSE、失败/重试、成功和取消分支。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。

### 已知限制

- API smoke 仍受本机旧开发进程占用文件监听器影响，本轮使用独立 3014 生产预览完成浏览器验收，未伪造 smoke 通过结论。

## 2026-09-06（P1-4 Inspector 抽屉组件化）

- 新增 `components/InspectorDrawer.tsx`，统一封装待办、计算、研究笔记、证据、日志、代码、结果和 3D 结构八类工具视图。
- 通过 `InspectorTab`、`InspectorNode` 和明确回调 Props 管理视图切换、证据来源、局部重试、结果血缘、残基选择与宽度拖拽，页面只负责编排状态和副作用。
- 保留 Knowledge Graph、代码流、火山图下载、3D 残基选择与 Mol* 降级提示等生命科学演示能力，未改变既有交互行为。

### 浏览器验收

- 真实打开代码、结果、3D 视图，结果血缘可回溯，残基 2 点击后出现“已选择”反馈。
- 证据来源和知识图谱仍可在抽屉中呈现，关闭工具抽屉按钮可正常响应；生产构建无白屏。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。

## 2026-09-06（P1-4 工作区弹窗组件化）

- 新增 `components/WorkspaceModal.tsx`，封装项目切换、能力说明、文件列表、新建任务、上传文件、布局切换和文件/证据详情弹窗。
- 统一文件选择、Enter 创建任务、空白区域关闭和确认反馈等交互，通过 Props 将状态修改回调交还页面编排。
- `WorkspaceModalState` 与 `ModalKind` 提供明确领域类型，避免弹窗分支继续堆积在 `app/page.tsx`。

### 浏览器验收

- 真实打开项目切换弹窗，选择“肿瘤基因组项目”后标题更新、弹窗关闭并出现“已切换到…” Toast。
- 保留上传文件名、布局重置、文件详情和证据详情的既有行为；生产构建无白屏。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。

### 接口验证边界

- 额外执行 `npm run smoke -- http://127.0.0.1:3014` 时，脚本在生产模式调用 `POST /api/tasks`，服务端按设计返回 `404 Disabled in production`。
- 该结果已如实记录为运行环境/脚本模式不匹配；鉴权登录本身通过，后续应在开发服务上执行完整状态变更 smoke，或为生产 smoke 增加只读分支。

## 2026-09-06（P1-5 类型化 API 适配层）

- 新增 `lib/domain.ts`，统一 `ResearchTask`、`WorkflowNodeState` 和任务响应领域类型。
- 新增 `lib/api-client.ts`，收口登录、任务读取、澄清提交、计划审批、配置读写、运行、取消和节点重试请求。
- 统一 JSON 解析与错误转换，页面不再重复判断 `response.ok` 或手动解析响应，服务端错误会转成中文 Toast 反馈。
- 保留现有 SSE 事件流用于实时日志和代码增量，API 客户端与事件流职责分离。

### 浏览器验收

- 刷新页面后登录、任务和配置读取正常。
- 真实走通“四步澄清 → 生成分析计划 → 审批 → 启动工作流”，状态依次更新为待审批、排队中、运行中。
- 配置面板保存后出现“演示配置已应用”，说明配置写入和任务刷新均经由新 API 适配层完成。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。

## 2026-09-06（P1-4 第一批组件化）

- 新增 `components/ClarificationCard.tsx`，集中维护四步澄清题库、答案类型、步骤导航和选项卡交互。
- `app/page.tsx` 的澄清区域改为受控组件，页面只负责答案状态、接口提交和步骤切换编排。
- 清理页面中一段不会参与渲染的重复澄清 JSX，降低后续拆分和代码 Review 成本。
- 浏览器真实点击通过：四步澄清、自动推进、生成分析计划并进入待审批状态。
- `npm run typecheck`、`npm run build`、`git diff --check` 和完整接口 smoke 均通过。

## 2026-09-06（P1-4 工作流画布组件化）

- 新增 `components/WorkflowCanvas.tsx`，封装工作流节点、SVG 连线、节点拖拽、画布平移、缩放、重置和 Shift 连线提示。
- 页面继续持有任务状态、节点位置和运行回调，组件只负责可视化与交互事件派发，便于后续替换 React Flow 或真实流程图引擎。
- 浏览器真实点击通过：打开分析计划、缩小、居中重置、点击“构建设计矩阵”打开证据抽屉。
- `npm run typecheck`、`npm run build`、`git diff --check` 和 `npm run smoke -- http://127.0.0.1:3012` 均通过。

## 2026-09-06（P1-4 任务侧栏组件化）

- 新增 `components/TaskSidebar.tsx`，封装项目切换、任务列表、数据集文件、上传入口和侧栏宽度拖拽。
- 组件 Props 使用 `activeTaskId`、`extraTaskNames`、`uploadedFileNames`、`onOpenProjectPicker` 等明确命名，避免把页面状态与展示结构混在一起。
- 页面统一负责任务切换、弹窗和文件详情状态，侧栏组件只派发用户意图。
- 浏览器真实点击通过：项目切换、CSV 文件详情、文献证据图谱任务跳转、上传入口和侧栏导航反馈。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过；接口 smoke 因旧开发进程造成 EMFILE/路由 404 未重复宣称通过，待清理旧进程后补跑。

## 2026-09-06（P1-1 RAG 与知识图谱可视化）

- 新增独立 `components/KnowledgeGraph.tsx`，将 Agent 检索证据按项目文件、技能包、文献、知识图谱四类分组，避免所有 RAG 逻辑堆在页面文件中。
- 每条证据展示相关度、绑定参数、输入、输出和对下一步分析的影响；点击来源会更新详情，突出生命科学分析的可解释链路。
- 增加处理组、设计矩阵、DESeq2、细胞周期、候选基因和文献依据关系图，支持点击图谱节点联动证据上下文。
- 完成阶段级浏览器回归：澄清、计划审批、运行、失败、证据、局部重试、代码、结果、3D、弹窗和抽屉均重新验收。
- `npm run typecheck`、`npm run build`、`git diff --check` 和 `npm run smoke -- http://127.0.0.1:3012` 均通过。

### P0-2 验收补充

- 五档断点（1440、1280、1024、768、390）已完成 CSS 合同审计；真实浏览器复核确认抽屉、遮罩、弹窗关闭、画布和输入区行为。
- 当前自动化接口无法直接改变系统窗口尺寸，因此未将模拟截图当作真实截图；该限制已记录在 `docs/待办工作流.md`，不影响代码中的响应式断点与交互逻辑。

## 2026-09-06（P0-1 全量回归）

- 完成提问、四步澄清、计划生成与审批、画布展开、运行、RAG 事件、失败原因、证据来源、局部重试、代码流、结果血缘、火山图下载、3D 残基联动和刷新恢复的浏览器真实点击验收。
- 完成能力中心、文件详情、项目切换、新建任务、上传入口、分享、布局、配置面板、Agent 模式、空消息提示、抽屉关闭和 Esc 关闭等可见交互验收。
- 扩展 `scripts/smoke-test.mjs`：覆盖未鉴权、错误澄清、配置读取与跨域写保护、完整 SSE 事件、失败节点、局部重试、成功分支、取消分支和产物持久化。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过；扩展 smoke 在干净的 3012 开发服务上通过，覆盖全部成功/失败/取消分支。生产构建期间曾触发开发缓存并发写入，已隔离损坏的 `.next` 缓存，后续构建需避免与开发服务并行。

### 已知限制

- 当前受沙箱限制无法结束外部启动的 3000 端口旧进程；该进程缓存损坏时会短暂返回 404，需在本机终端结束旧进程后重新执行 `npm run dev`。默认端口配置仍保持 3000。

## 2026-09-06（P0-2 响应式溢出修复）

- 修复检查器抽屉关闭状态仍位于视口右侧导致的横向滚动条，统一对根页面启用横向溢出裁剪。
- 浏览器生产预览已验证粒子加载页、工作台就绪、当前桌面视口无可见横向滚动；响应式断点规则覆盖 1200px、760px、560px 和 1500px。

## 2026-09-04（第二轮）

### 运行闭环与鉴权增强

#### 产品与交互

- 将任务节点状态从前端临时状态迁移为服务端运行状态。
- 增加真实的运行器状态推进：启动、节点运行、失败、局部重试、完成和取消。
- 增加 Evidence Pipeline、运行日志、结果 Artifact 和 SSE 事件的统一关联。
- 增加 Results Inspector：火山图指标、产物列表和结果血缘回溯。
- 增加取消运行入口，明确取消是异步请求并保持状态反馈。

#### 技术实现

- 新增 `lib/store.ts`，集中管理 Demo 任务快照、事件游标、节点运行和 Artifact 状态。
- 新增 `/api/runs/[runId]/events` SSE 事件接口，支持 `after` 游标增量读取。
- 新增 `/api/runs/[runId]/cancel` 取消接口。
- 新增 `/api/runs/[runId]/nodes/[nodeId]/retry` 节点局部重试接口。
- 前端通过 EventSource 监听运行事件，并在事件到达后重新同步服务端任务快照。
- 会话 Cookie 改为带过期时间的 HMAC 签名 Token，并设置 HttpOnly、SameSite=Strict。
- 所有写操作 API 增加同源 Origin 校验。
- 增加 `results` Artifact viewer 和内嵌 SVG 火山图 Demo。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。

## 2026-09-05（界面统一）

- 将智能体澄清卡片从深色开发控制台样式调整为暖灰纸张主题，统一边框、按钮、表单和提示层级。
- 将澄清阶段的标题、说明、字段、选项和 CTA 改为中文，强化“分析上下文确认”的产品语义。
- 将右侧 Inspector 与顶部操作栏的常用英文标签替换为中文，降低生命科学研究工作台的认知割裂。

### 修复：清理 Next.js 构建缓存导致的模块缺失

- 将旧 `.next` 缓存移动到系统临时目录并重新执行生产构建，修复 `Cannot find module './682.js'` 这类热更新残留问题。
- 统一 Evidence Pipeline 默认阶段名称为中文：识别研究意图、重写分析问题、检索与证据重排、绑定分析依据。
- 当前沙箱禁止监听本地 3000 端口，已确认属于环境权限限制；生产构建本身正常通过。
- README 增加旧端口进程与 404 的排查命令，优先使用生产预览减少热更新缓存问题。
- `package.json` 增加 `npm run preview`，一条命令完成构建并启动生产预览，规避开发热更新监听器导致的 404/EMFILE。
- 将计划审批卡片与中央工作流标题进一步中文化，统一面试演示中的研究流程表达。

## 2026-09-05（中屏适配）

- 增加 761–1200px 中屏布局：收窄任务侧栏、压缩标题和表单间距、调整检查器抽屉宽度。
- 检查器打开时新增全屏半透明遮罩，点击空白区域即可关闭，避免弹层遮挡后无法退出。
- 保留移动端底部操作入口和大屏三栏布局，形成大屏 / 中屏 / 移动端三档响应式策略。

## 2026-09-05（中屏视觉收口）

- 修复中屏下内联四栏网格样式覆盖媒体查询，移除检查器隐藏后右侧残留的大块空白。
- 中屏布局强制使用“导航 + 任务栏 + 工作区”三列，检查器改为浮层抽屉，不再挤压主工作区。
- 将顶部、证据流水线、结果产物和检查器中的主要英文文案统一为中文，避免中英文混排造成认知割裂。
- 默认预览入口保持 `3000` 端口。

## 2026-09-05（多端布局二次修复）

- 针对中等宽度桌面补充 1201–1500px 适配，避免主工作区右侧出现大面积空白。
- 中等宽度下检查器统一改为抽屉浮层，主工作区使用剩余宽度自适应。
- 清理证据流水线、顶部导航和检查器中的英文残留，统一为中文界面。

## 2026-09-05（根因修复）

- 修复内联 `gridTemplateColumns` 覆盖响应式媒体查询的问题：视口宽度不超过 1500px 时动态移除检查器占位列。
- 中屏主工作区不再出现右侧大面积空白，检查器只在打开时以抽屉浮层显示。

## 2026-09-05（产品逆向校正）

- 新增 `analysis.md`，按作业文档补齐 Biomni 的目标用户、核心路径、页面结构、状态、功能取舍和降级方案。
- 明确产品主叙事调整为“对话优先、计划/画布次级、工具抽屉按需打开”。
- 增加视口状态驱动的布局列数计算，避免 React 内联四栏样式覆盖中屏抽屉布局。

## 2026-09-05（构建环境）

- 增加 `.nvmrc`，锁定 Node.js 20 LTS；README 补充 Node 24 与 Next.js 14 的已知兼容问题及处理方式。

## 2026-09-05（对话式澄清）

- 将四个横向下拉框重构为分步单选卡片，说明每个选择对分析流程、参考基因组和交付物的影响。
- 增加澄清步骤导航、已完成状态、自动进入下一问题和动态完成计数。
- 证据流水线默认收起，通过 Agent 消息内的执行轨迹按钮展开或关闭。
- 移动端将步骤导航和问题内容切换为双列/单列布局，保持触控可用性。

## 2026-09-05（研究工具抽屉）

- 将固定 Inspector 改为全尺寸默认关闭的研究工具抽屉，彻底取消主工作区的右侧占位列。
- 增加待办、结果、计算和笔记工具坞；待办与澄清进度联动，计算面板展示环境与数据边界。
- 保留证据、日志、代码和 3D 上下文标签，并增加统一的抽屉标题。
- 支持点击遮罩、关闭按钮和 `Esc` 三种关闭方式；移动端工具坞切换为底部横排。

## 2026-09-05（工作流画布交互）

- 研究工具抽屉增加待办、结果、计算、笔记四类入口，并与任务上下文联动。
- 工具抽屉不再占用主工作区列，统一支持遮罩、关闭按钮和 `Esc` 退出。
- 为下一阶段节点拖拽、画布平移和缩放保留独立工具层，避免画布能力反向绑架对话主流程。

## 2026-09-06（可交互工作流画布）

- 实现节点拖拽，节点位置更新时关联连线实时跟随。
- 支持拖拽画布空白区域进行平移，并兼容触控指针事件。
- 支持滚轮缩放、工具栏放大/缩小，缩放范围限制为 55%–160%。
- 增加一键居中重置，同时恢复默认节点布局、缩放比例和画布偏移。
- 节点轻点仍可选中并打开证据工具，拖拽不会修改服务端任务运行状态。

## 2026-09-06（面试材料收口）

- 新增 `docs/AI使用说明.md`，记录 AI 辅助范围、上下文、人工 Review、问题修正与验证方式。
- 新增 `docs/演示脚本.md`，整理约 6 分钟的 Biomni 逆向演示路径。
- README 增加产品逆向、技术方案、AI 使用说明和演示脚本入口。

## 2026-09-06（科研问题发送）

- 输入框接入真实前端状态，支持发送按钮和 Enter 快捷发送。
- 新发送的问题会追加到当前对话时间线，保留用户消息的科研上下文。
- 保留服务端 Agent 运行 API，发送行为不绕过鉴权，也不会直接修改任务节点状态。

## 2026-09-06（交互反馈增强）

- 发送科研问题后即时追加用户消息气泡，并在短延迟后展示智能体确认回复。
- 增加消息进入动画、工具按钮按压反馈和画布工具按钮反馈，强化操作结果的可感知性。
- 画布收起时增加明确的引导提示，告诉用户可通过“分析计划”重新打开可编辑画布。

## 2026-09-06（画布连线编排）

- 支持 Shift+点击两个节点创建临时连线，连线会立即显示在画布上。
- 连线源节点增加脉冲高亮，工具栏实时提示“正在连线”和下一步操作。
- 连线编辑仅作用于当前前端视图，不改写服务端任务状态，便于演示安全撤销和重置。

## 2026-09-05（对话优先重构）

- 首屏新增用户问题与 BioFlow 智能体回复时间线，科研任务从对话自然进入文件检查和澄清阶段。
- 将证据检索入口放进 Agent 回复，以“已检查项目文件”Trace 连接 RAG 过程。
- 工作流画布默认收起，通过“分析计划”按需展开，不再抢占首屏主叙事。
- 失败信息仅在任务失败时进入对话；输入框改为中文，并固定为中央任务区的持续操作入口。

## 2026-09-05（第七轮）

### 服务端代码流式事件

#### 产品与交互

- 将 Code Inspector 的代码生成过程纳入 Agent 运行事件链，代码不再只依赖前端本地模拟。
- 节点重试时先清空旧代码，再按服务端事件增量渲染新的分析脚本。
- 代码 Artifact 会和 DESeq2 节点、运行日志、结果产物保持同一条血缘链。

#### 技术实现

- `lib/store.ts` 新增 `emitCode`，通过 `code.delta` 事件逐字符发布分析脚本。
- 前端 SSE `onmessage` 处理 `code.delta`，增量更新 `codeText`。
- 重试接口增加 `artifact_code` 产物记录。
- 保留前端首轮 Demo 代码输出作为无事件时的可用降级展示。

## 2026-09-05（第八轮）

### RAG Evidence Pipeline 服务端事件化

#### 产品与交互

- Agent 运行时新增意图识别、检索启动、检索命中、证据重排和 grounding 事件。
- Evidence Pipeline 根据 SSE 实时事件更新阶段状态，不再只依赖静态种子数据。
- 检索阶段展示项目文件、Skills 和文献命中数量。
- Grounding 阶段展示已绑定的统计参数，强化“证据影响计划”的可解释性。
- Code Inspector 与 Evidence Pipeline 现在共享同一条服务端事件流。

#### 技术实现

- `lib/store.ts` 在工作流启动时发布 `intent.detected`、`retrieval.started`、`retrieval.hit`、`evidence.reranked` 和 `grounding.bound`。
- 前端 EventSource 增加事件类型映射，根据服务端 payload 更新 Evidence 卡片。
- 保持统一事件游标和任务快照同步机制。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。

#### 已知限制

- 当前 Store 为单进程 Demo 持久层，生产环境需替换为 SQLite/Postgres。
- SSE 运行器为确定性 Mock，不执行真实 RNA-seq 计算。

## 2026-09-04（第三轮）

### 持久化与计划审批接口

#### 产品与交互

- 为任务状态增加 clarification 数据，保留格式、比较组、物种和交付物选择。
- 增加从澄清答案生成计划的服务端接口，为后续“等待审批”页面接入做好准备。
- 增加独立的工作流计划审批接口，避免把计划批准和运行启动混为一步。
- 运行失败、局部重试、取消和结果完成状态均写入统一 Store。

#### 技术实现

- `lib/store.ts` 增加本地 `data/state.json` 文件持久化；在只读部署环境自动降级到进程内存。
- 增加 `/api/tasks/:taskId/clarifications`，校验澄清项完整性并生成计划事件。
- 增加 `/api/workflows/:workflowId/approve`，写入计划审批事件。
- 节点、运行、Artifact 和事件变更统一触发持久化。
- 新增 `data/state.json` 到忽略列表，避免把本地运行数据提交进仓库。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过，新增接口均被 Next.js 正确识别。

#### 已知限制

- 文件持久化适合单机面试 Demo，多进程生产环境需替换 SQLite/Postgres 和队列。
- 澄清与审批接口已完成，前端审批卡片将在下一轮接入主流程。

## 2026-09-04（第四轮）

### 接入前端澄清、计划审批与流式代码

#### 产品与交互

- 前端新增四项结构化澄清：数据格式、比较组、物种和交付物。
- 澄清未完成时，工作流保持门控状态，不能直接运行。
- 澄清提交后显示 6 步计划预览、预计耗时、证据数量和外部数据传输提示。
- 计划审批与工作流运行拆成两个用户动作，体现 Human-in-the-loop。
- Inspector 日志改为实时消费 SSE 事件并保留最近 100 条。
- Code Inspector 增加逐字符增量输出，模拟 Agent 代码生成过程。

#### 技术实现

- 前端接入 `/api/tasks/:taskId/clarifications` 和 `/api/workflows/:workflowId/approve`。
- 默认任务状态改为 `clarifying`，由澄清提交推动到 `awaiting_approval`。
- 计划批准后进入 `queued`，运行按钮在门控状态下自动禁用。
- 新增 `codeText` 增量状态，执行时逐步渲染分析代码。
- SSE `onmessage` 同步服务端任务快照并追加事件日志。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。

#### 已知限制

- 代码流当前是前端增量演示，下一步可改为服务端 `code.delta` SSE 事件。
- 3D Viewer 当前仍为轻量降级预览，真实 Mol* 集成待后续版本。

## 2026-09-04（第五轮）

### Biomni 风格生命科学视觉重构

#### 产品与交互

- 根据竞品页面复盘，将产品从通用深色开发控制台调整为生命科学研究工作台。
- 保留 Agent Evidence Pipeline、工作流画布和动态 Inspector，但降低画布对主界面的压迫感。
- 强化 RNA-seq 领域语义：数据读取、样本质控、设计矩阵、DESeq2、显著基因、火山图和科研报告。
- 任务侧栏、数据集、状态文案和研究目标统一使用中文科研语境。
- 视觉基调改为暖灰纸张、石墨文字、荧光黄绿状态色，接近 Biomni 的实验室/研究产品气质。
- 节点状态同时使用图标、文字、边框和颜色，提升可扫描性与可访问性。

#### 技术实现

- 更新 `lib/store.ts` 默认 RNA-seq 节点名称、状态说明和任务目标。
- 更新 `app/page.tsx` 侧栏、任务头部、状态标签和主流程文案。
- 在 `app/globals.css` 增加 Biomni-inspired visual theme override，覆盖桌面、平板和移动端控件样式。
- 保留现有鉴权、SSE、持久化、澄清、审批、重试、取消和 Artifact 逻辑。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。

#### 设计复盘

- 不再追求一比一复刻 Biomni 布局；改为借鉴其研究工作台气质，并把 Agent 证据链和结果血缘作为差异化能力。

## 2026-09-05（第六轮）

### 工作台可调布局交互

#### 产品与交互

- 左侧任务/数据栏支持横向拖拽调整宽度，范围 210–390px。
- 右侧 Evidence/Logs/Code/Results 检查器支持横向拖拽调整宽度，范围 260–520px。
- 上方 Evidence Pipeline 支持纵向拖拽调整高度，范围 70–220px。
- 拖拽过程中显示边界高亮，避免用户不知道当前可操作区域。
- 统一处理 `pointerdown / pointermove / pointerup`，支持触控板、鼠标和触摸输入。
- 拖拽时禁用文本选择，避免误选页面内容；移动端继续使用 Inspector 抽屉，不强制显示桌面分隔线。

#### 技术实现

- `app/page.tsx` 新增 `sidebarWidth`、`inspectorWidth`、`evidenceHeight` 和 `resizing` UI 状态。
- 使用 CSS Grid 内联变量实时更新三栏宽度。
- 新增左侧、右侧和水平 splitter 元素与统一 `startResize` 处理器。
- 增加最小/最大尺寸约束和 `touch-action: none`。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。

## 2026-09-05（白屏问题修复）

### 本地启动与演示状态修复

#### 问题原因

- 排查确认白屏不是 React 渲染异常，而是 Chrome 打开的 `localhost:3000` 没有对应的 Next 开发进程。
- 本地 `data/state.json` 还保留了上一次运行后的旧英文失败快照，会遮蔽新版中文初始状态。

#### 改动

- 删除本地临时运行快照，使 Store 回到新版中文澄清初始状态。
- Next 开发和生产脚本明确绑定 `127.0.0.1`，降低本机监听地址差异造成的访问问题。
- 增加 `README.md` 白屏排查说明和 `npm run dev:poll` 文件监听降级命令。

#### 验证

- 本地服务启动后 `GET /` 返回 `200 OK`。
- 页面可正常返回 BioFlow Studio HTML 和客户端脚本。

## 2026-09-04

### 初始版本：搭建可解释科研 Agent 工作台

#### 产品与交互

- 将 Biomni 逆向方案定位为“可解释、可干预、可恢复的科研 AI 工作台”。
- 搭建任务侧栏、工作流画布、Evidence Pipeline、动态 Inspector 四层交互结构。
- 增加 RNA-seq 差异表达 Demo 工作流，覆盖读取数据、质量检查、设计矩阵、DESeq2、火山图和报告节点。
- 增加节点成功、运行中、阻塞、失败等状态表达。
- 增加失败节点的原因解释、下游影响提示和局部重试交互。
- 增加 Evidence、Logs、Code、3D 四种 Inspector 视图。
- 增加代码执行状态、3D 结构预览降级方案和移动端 Inspector 抽屉。
- 增加桌面端、平板端和移动端响应式布局。

#### 技术实现

- 初始化 Next.js 14 + React 18 + TypeScript 项目。
- 增加服务端 Cookie 会话鉴权，保护任务和运行 API。
- 增加 `/api/auth/login`、`/api/auth/logout`、`/api/tasks`、`/api/runs`。
- 增加服务端 Mock 运行入口，为后续 SQLite、SSE 和真实 Agent Adapter 预留接口。
- 增加 `.env.example`，明确模型密钥只能放在服务端环境变量。
- 增加 README 启动说明和安全边界说明。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 已忽略本地依赖、构建缓存、临时目录和环境变量文件。

#### 已知限制

- 当前使用确定性 Mock 数据，尚未接入 SQLite、SSE 和真实 LLM。
- 3D 结构当前为轻量降级预览，后续接入 Mol* 或 3Dmol.js。
- 本地沙箱限制端口监听，浏览器级 localhost 预览需在开发机执行。

## 2026-09-05

### 交互：增强蛋白质结构 Artifact 预览

- 为 3D 结构面板增加拖拽旋转反馈，使用指针事件适配鼠标与触控板操作。
- 增加残基点击识别与高亮状态，为后续联动证据、知识图谱和代码上下文保留状态入口。
- 增加结构置信度（pLDDT）和“拖拽旋转”操作提示，强化生命科学场景语义。
- 保留轻量静态降级方案，后续可将同一交互容器替换为 Mol* / 3Dmol.js。

#### 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。
# 2026-09-06

## 计划：建立后续待办工作流

- 新增 `docs/待办工作流.md`，按 P0/P1/P2/P3 划分演示闭环、RAG、真实文件解析、组件化、Artifact、3D 和交付材料。
- 为每个阶段补充目标、执行顺序、验收标准和统一完成门槛，后续开发按文档逐项推进并在浏览器验收。
- 增加阶段级全量回归门禁：阶段完成前必须复测已有核心流程、全部可见交互、API/SSE、五种视口和工程质量检查，发现核心回归失败时不得标记阶段完成。

## 工程化：配置驱动演示引擎与粒子化加载页

- 新增 `lib/demo-config.ts`，集中定义工作流名称、研究目标、样本/基因规模、失败分支和运行节奏，避免业务参数散落在页面与 Runner 中。
- 新增受鉴权保护的 `/api/demo/config` 读写接口；页面“配置”面板可实时切换失败恢复链路或全成功链路，保存后由服务端状态驱动下一次运行。
- 将演示 Runner 的事件域、运行等待、代码流式输出间隔和结果统计接入配置，支持面试现场演示不同执行路径。
- 新增 `components/ParticleLoader.tsx`，使用 Canvas 粒子环和渐变光晕表现科研数据初始化过程，并增加最小加载时长，避免快速请求时动画一闪而过。
- 新增 `scripts/start-demo.mjs`，`npm run dev` 默认使用 3000 端口并尝试释放占用进程；补充 README 的配置化演示与端口说明。
- 将配置面板和加载动画抽离为独立组件，降低页面单体复杂度。

### 验证

- 浏览器验收配置面板：打开、切换“不失败（演示成功链路）”、保存并显示 Toast。
- 浏览器截图验收 Canvas 粒子加载页，粒子范围、亮度和加载提示符合需求文档方向。
- `npm run typecheck`、`git diff --check`、`node --check scripts/start-demo.mjs` 通过。

## 交互：补齐全站可见控件反馈与运行状态闭环

- 将证据来源、结果 SVG 下载、3D 残基、Mol* 入口改为源码级可访问按钮，点击均有弹窗、下载或 Toast 反馈。
- 将证据抽屉中的状态、说明、来源、错误卡片和 3D 预览文案统一为中文，去除依赖 DOM 文本替换造成的时序风险。
- 为待办面板条目、结果血缘追溯、研究工具抽屉和分析计划审批补充明确的定位与反馈。
- 延长 Mock Runner 失败节点出现前的运行窗口，确保“取消”按钮可被真实点击；取消后同步“已取消”状态与提示。
- 将失败重试闭环验收至“重试中 → 已完成 100%”，并回归“待补充信息 → 待审批 → 排队中 → 运行中 → 失败”。

### 浏览器验收

- 在临时 QA 页面逐项点击导航、任务、文件、上传、项目、布局、澄清、审批、失败原因、局部重试、工具抽屉、结果下载、3D 残基与 Mol* 入口。
- 确认运行状态通过轮询/SSE 自动同步，失败节点和证据抽屉中文状态正常显示。
- `npm run typecheck`、`git diff --check` 通过；生产构建受本机旧 Next 进程和文件句柄限制影响未完成，已停止临时 QA 进程并保留默认 3000 端口配置。

## 完成最终演示收口

- 将 Agent Runner 的运行事件、失败原因、重试状态和产物提示统一为中文，保留 RNA-seq、DESeq2、FDR 等必要领域术语。
- 新增 `npm run smoke` 脚本入口，生产预览下自动跳过仅开发可用的状态重置接口。
- README 补充演示验收清单、架构图、运行时序和 Mock 边界说明。
- 使用 Node.js 22 完成 `npm run build`，并在 3011 端口完成首屏无白屏浏览器回归；`npm run smoke -- http://127.0.0.1:3010` 通过。

## 修复收起计划提示窄列换行

- 让“分析计划已收起”提示跨越对话区完整网格，避免落入 30px 头像列后逐字换行。
- 统一动态用户消息、智能体回复和失败消息占据对话区整行。

## 修复 Next.js 依赖模块加载错误

- 重新安装被 macOS/iCloud 标记为 `dataless` 的 Next.js 内置依赖，恢复 `comment-json.parse` 与 `semver.lt`。
- 验证 `npm run typecheck` 通过。
- 开发环境如遇 `EMFILE` 文件监听上限，使用 `npm run dev:poll` 启动轮询模式。
