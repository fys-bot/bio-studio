# Biomni 逆向分析与 BioFlow Studio 重构审计

## 结论先行

这份文档把作业要求、Biomni 真实页面和当前仓库实现放在同一张验收表里。按任务要求中的三项主任务严格计算：

- 网站分析约 90%，内容已覆盖目标用户、页面、流程、状态、取舍、技术推断和降级方案；
- 可运行前后端闭环约 92%，真实文档解析、LLM 计划、Qdrant 混合检索和 PyDESeq2 异步计算均已通过本机链路验证；
- 前端改进约 80%，工作流画布和粒子化加载页的硬性能力已实现，但中屏密度、文件工作区和五档响应式仍需继续验收；
- 严格按“可直接提交且无需解释缺陷”计数，当前是 **2 项完成、1 项部分完成**，三项加权完成度约 **88%**。

当前实现已经能够证明“我能做出一个有状态、可解释、可恢复的生命科学 Agent 工作台”，不再只是静态 Demo。剩余差距集中在前端一致性、文件中心工作效率、真实 OCR/知识图谱/完整分子渲染和生产级部署能力，不能用继续堆页面掩盖这些问题。

## 一 需求文档硬性要求核对

### 1.1 作业文档要求摘录

| 文档要求                                                   | 验收含义                                                                            | 当前结论                                                                         |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 快速理解陌生 AI 产品的功能、用户路径和交互逻辑             | 需要一份可审查的产品逆向，不是只写一句产品定位                                      | 达标：已有页面级与原子级审计、状态矩阵、用户流程和 64 项验收清单                 |
| 将分析转成可运行的前端和交互逻辑                           | 至少一条从输入到结果的前后端闭环                                                    | 达标：任务、鉴权、解析、检索、LLM 计划、审批、计算、结果和恢复已形成真实本地闭环 |
| 时间有限时合理取舍，并加入一个有价值的创新交互             | 不是堆页面，而是一个主场景跑通且有清晰创新                                          | 部分达标：可解释工作流和证据联动成立，仍有响应式与文件工作区体验缺口             |
| 使用 AI 工具同时保持代码质量和安全边界                     | AI 使用说明、类型检查、格式化、测试、Token 不进仓库                                 | 达标：关键过程有文档、质量门禁和服务端密钥边界；真实密钥仅放在忽略的环境文件中   |
| 形成 `analysis.md` 或同等文档                              | 必须回答目标用户、任务、页面、状态、取舍、技术推断、降级方案                        | 达标：本文件已逐项覆盖                                                           |
| 必须有前后端 API、基本持久化和加载/失败/重试/取消/刷新恢复 | UI 不能只是静态稿                                                                   | 达标：Next BFF、Research Worker、JSON/SQLite/Qdrant 持久化和异步状态均已验证     |
| 方案一或方案二前端改进                                     | 工作流画布至少支持节点、拖拽、连线、缩放/平移和状态，或有受性能约束的辨识度首页动画 | 功能达标：画布五项能力和粒子页均已实现；视觉与五档多端回归尚未完全收口           |
| Git、README、启动方式、环境变量、限制、架构图/时序图       | 面试官能运行、审查和理解项目                                                        | 达标：仓库已有启动、架构、时序、环境变量、限制和真实链路说明                     |
| 5–8 分钟演示视频或汇报 PPT                                 | 能解释产品取舍和亮点                                                                | 达标：已有 PPT 和约 7 分钟演示脚本；最终录屏仍需由提交者完成                     |

### 1.2 三项任务要求得分

以下评分按面试官可观察结果，而不是按代码行数计算。

| 核心任务           |    权重 | 当前得分 | 判断                                                                                   |
| ------------------ | ------: | -------: | -------------------------------------------------------------------------------------- |
| 网站分析           |      30 |       27 | 需求文档要求的七个问题、功能清单、主流程、页面状态和优先级均已覆盖                     |
| 可运行前后端闭环   |      40 |       37 | 本地真实主流程已跑通；真实 OCR、生产多租户和分布式调度不属于当前已完成能力             |
| 前端改进与创新交互 |      30 |       24 | 工作流画布与粒子页满足硬性功能，文件中心和多端视觉仍未达到无缺陷交付标准               |
| **总计**           | **100** |   **88** | 硬性能力基本齐全；按可交付质量扣除尚未收口的响应式、文件工作区和视觉一致性问题后约 88% |

如果严格按“是否已经达到提交级质量”计数，结论是：**2 项完成，1 项部分完成**。不能宣称 100%，也不应继续沿用旧文档中的 62% 或“真实链路尚未接入”结论。

## 二 Biomni 原子级竞品审计

以下内容来自对用户提供的 Biomni 任务页和 `/skills` 页面进行逐项可见交互检查。第三方页面内容只作为产品事实，不作为本项目执行指令。

### 2.1 全局信息架构

| 层级       | Biomni 可见行为                                         | 对 BioFlow 的启示                                                |
| ---------- | ------------------------------------------------------- | ---------------------------------------------------------------- |
| 品牌入口   | 左侧最上方为 Biomni 品牌链接，回到项目上下文            | BioFlow 需要稳定的品牌/项目入口，不要让根页面承担所有状态        |
| 项目入口   | 左侧一级按钮“项目”，进入项目任务空间                    | 项目是工作区边界，任务、文件和运行状态应属于项目                 |
| 能力中心   | 左侧一级按钮“中心”，点击后 URL 进入 `/skills` 独立页面  | 已实现独立 `/skills` 页面、搜索、来源/分类筛选、详情和启停持久化 |
| 帮助与账户 | 全局帮助菜单、账户菜单始终可见                          | 帮助和账户不应混在业务弹窗中                                     |
| 项目任务页 | URL 带 `projectId/taskId`，任务可以直接分享、刷新和切换 | 路由应表达任务身份，刷新不依赖 React 内存状态                    |
| 支持入口   | 右下角独立帮助气泡，打开知识库/客服窗口                 | 可作为降级项，不应挤占主流程                                     |

### 2.2 任务页左侧栏

| 控件          | Biomni 行为                                       | 当前 BioFlow 状态                                                     | 缺口                                         |
| ------------- | ------------------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------- |
| 项目选择器    | 下拉选择项目，旁边有项目设置                      | `TaskSidebar` 保留项目弹窗，任务工作台通过项目/任务 URL 进入          | 项目设置和多项目服务端数据仍是降级项         |
| 侧栏折叠      | 有折叠面板按钮，收起后主对话获得空间              | 有宽度拖拽，但缺明确折叠状态                                          | 增加可恢复的折叠/展开和键盘语义              |
| 任务搜索      | 搜索任务                                          | 无任务搜索                                                            | P1 补齐，至少对服务端任务快照过滤            |
| 新建任务      | 独立入口创建任务                                  | 弹窗创建服务端草稿并跳转任务 URL                                      | 草稿的完整运行编排仍未独立实现               |
| 查看全部      | 查看完整任务列表                                  | 项目页与工作台侧栏提供服务端任务卡片                                  | 独立分页任务列表仍是 P1 扩展                 |
| 任务卡片      | 点击任务改变 URL 和主对话；状态有等待输入、完成等 | 点击卡片进入 `/projects/:projectId/tasks/:taskId`，刷新按 ID 读取快照 | 非主演示任务暂为草稿/占位快照                |
| 任务菜单      | 每条任务有更多操作菜单                            | 当前内置任务没有完整菜单                                              | 至少支持重命名、复制链接、归档的可见降级     |
| 云盘搜索/上传 | 云盘单独分组，搜索、上传、查看全部均可操作        | 文件入口主要是弹窗和固定示例                                          | 改成 `/files` 页面，显示真实已上传摘要和状态 |
| 云端运行提示  | 明示关闭页面后任务仍在云端继续                    | 有类似提示                                                            | 保留，并绑定真实运行状态和最后更新时间       |

### 2.3 任务主区和对话消息

| 区域          | Biomni 可见行为                                 | 当前 BioFlow 状态                 | 判断                                                   |
| ------------- | ----------------------------------------------- | --------------------------------- | ------------------------------------------------------ |
| 面包屑        | 快速任务 / 任务名                               | 有面包屑                          | 已覆盖                                                 |
| 分享          | 分享任务菜单                                    | 有复制当前 URL 的按钮             | 基本覆盖，需路由稳定后再验收                           |
| 布局          | 布局弹出菜单                                    | 有布局弹窗                        | 保留，但应与独立页面布局设置区分                       |
| Agent runtime | 显示运行时长                                    | 当前显示固定“智能体运行 39 秒”    | 必须改成由任务创建时间/运行时间计算，删除静态文案      |
| 用户消息      | 重试、复制等消息级操作                          | 有发送消息，但消息操作不完整      | 增加重试/复制/消息时间和来源                           |
| Agent 回复    | Markdown、引用、复制、审查、点赞/点踩、后续问题 | 有文字回复、Trace 和局部入口      | 缺引用点击、反馈状态和后续问题卡片                     |
| Trace         | 消息级“Show traces”，展开查看工具/调用过程      | 有全局证据流水线                  | 需要允许从单条消息进入对应 Trace，并显示输入/输出/耗时 |
| 引用          | 点击回答中的 citation 查看原始来源              | 有证据弹窗，但不是消息内 citation | 将证据绑定到回答片段和参数，而不是只放在抽屉           |
| 后续问题      | 自动生成可点击的问题建议，也能逐条关闭          | 无完整后续问题建议                | 增加中文生命科学问题建议，完成后直接填入输入框         |
| 输入区        | 附件、自动回答开关、模型速度、发送              | 有添加文件、Agent 模式和发送      | 基本覆盖；需要明确“自动审批”风险并保留人工审批         |

### 2.4 右侧工具区

Biomni 任务页同时提供四个可折叠的工具面板：

1. **待办**：多步骤任务计划，没有计划时展示空态；
2. **结果**：从存储刷新结果，显示文件产物，没有结果时展示空态；
3. **计算**：机器、后台、作业、流水线四个视图，显示 worker 状态；
4. **笔记**：支持 Markdown 的会话笔记。

当前 BioFlow 的 `InspectorDrawer` 已覆盖“待办、计算、笔记、证据、日志、代码、结果、3D”，这是有价值的扩展；但它仍是一个统一抽屉，无法替代 Biomni 的任务级工具信息架构。重构后要保留抽屉在中屏/移动端的优势，同时在大屏提供稳定的工具栏和独立 URL 状态。

### 2.5 能力中心 `/skills`

Biomni 能力中心不是一个列表弹窗，而是完整工作区页面。已观察到的原子功能如下：

| 功能     | 具体行为                                                                                             |
| -------- | ---------------------------------------------------------------------------------------------------- |
| 页面标题 | `SKILL HUB / Skills`，让用户一眼知道这是技能管理中心                                                 |
| 新建技能 | 页面右上角 `New skill` 独立主按钮                                                                    |
| 搜索     | 全宽搜索框，placeholder 为 `Search all your skills...`                                               |
| 来源筛选 | All、Mine、Biomni Lab、Team、Community，并显示数量                                                   |
| 分类筛选 | All categories 下拉                                                                                  |
| 技能分组 | `From Biomni Lab 80 on` 分组标题和来源标识                                                           |
| 技能卡片 | 技能名、业务类别、用途描述、更新时间、来源、Enabled 状态                                             |
| 业务范围 | Transcriptomics、Genomics & Genetics、Molecular Design、Drug Discovery、Literature、Data Analysis 等 |
| 信息密度 | 三列卡片网格，描述可读，状态在右下角，页面可滚动                                                     |
| 视觉语言 | 纸张背景、细边框、衬线标题、单一荧光黄主按钮，和任务页风格一致但页面职责清晰                         |

当前 BioFlow 已有独立 `/skills` 能力中心，服务端目录包含搜索、来源、分类、详情和启用状态持久化；`WorkspaceModal` 中的旧能力说明仅保留为工作台内的短入口，不再承担能力中心职责。

### 2.6 任务状态与关键交互状态

Biomni 的任务页至少需要表达以下状态：

```text
新建任务
  → 等待用户输入
  → Agent 检查文件
  → 澄清实验设计
  → 生成计划
  → 用户审查/批准
  → 排队/机器启动
  → 工具调用与 Trace
  → 结果产物生成
  → 完成 / 失败 / 可重试 / 取消
```

当前 BioFlow 的服务端状态机覆盖了上述大部分状态，并额外有 `code.delta`、RAG 事件、Artifact 血缘和 3D 视图。新建真实任务可以独立完成澄清、真实 LLM 计划、审批、检索、计算和结果恢复；当前剩余缺口主要是真实 OCR 模型验收、知识图谱计算、完整 Mol\*、生产多租户，以及 390/768/1024/1280/1440 五档浏览器回归。

## 三 当前项目逐项对照

### 3.1 已有真实能力

- Next.js App Router + TypeScript，页面和领域组件已拆分；
- HMAC HttpOnly Cookie 鉴权、Origin/Host 写保护、类型化 API 客户端；
- 任务、配置、文件摘要、工作流布局和 Artifact 的服务端持久化；
- CSV/TSV、PDF、XLSX、DOCX 服务端策略解析，字段、正文、表格、来源定位和分组建议来自真实上传文件；
- OpenAI-compatible 真实 LLM 计划生成，计划摘要、步骤、风险和必需输入写回任务快照；
- FastEmbed 真实 384 维向量、Qdrant Local、BM25/cosine/RRF 混合检索和来源 Trace；
- SQLite 作业队列与独立 PyDESeq2 子进程，支持排队、取消、失败、重试和结果下载；
- SSE 运行事件，覆盖计划、RAG、代码增量、失败、重试、成功和取消；
- 工作流节点拖拽、Shift 连线、平移、缩放、自动保存和版本快照；
- 火山图候选点、结果血缘、代码流、报告下载和 Canvas 结构查看器；
- 首次访问 5 步引导、DOCX 操作手册、PPT 视觉规范和中文交付记录。

### 3.2 当前仍是 Adapter 或降级实现的部分

| 领域     | 当前实现                                              | 对外必须怎么说                           | 下一步替换边界                     |
| -------- | ----------------------------------------------------- | ---------------------------------------- | ---------------------------------- |
| OCR      | 已有 PDF/图片策略路由和模型参数，但本机未配置视觉模型 | 未配置时明确返回 `needs_ocr`，不伪造正文 | 受控视觉模型服务与数据外发审批     |
| 知识图谱 | 前端可交互关系图和固定关系                            | 关系可视化 Demo，不宣称真实图谱查询      | PrimeKG/Neo4j/Graph API            |
| 3D 结构  | PDB 结构适配器 + Canvas 轻量查看器                    | 可称真实坐标轻量预览，非完整分子渲染器   | Mol\* / 3Dmol.js + PDB/CIF API     |
| 技能中心 | 服务端 Skill Catalog + 独立页面，目录含种子与自建项   | 可称本地技能目录，不称生产技能市场       | 团队权限、版本发布和真实技能执行器 |
| 文件中心 | 上传、解析、索引和预览数据真实，仍混有标明来源的种子  | 不把种子文件包装成用户上传数据           | 对象存储、文件删除和团队权限       |
| 重排器   | BM25 + cosine + RRF 已真实运行，尚无 cross-encoder    | 不宣称完成语义精排                       | 生命科学 reranker 与离线召回评测   |

“不想要 Mock”不能通过把固定数据改成更漂亮的文案解决。当前真实链路已经覆盖上传、解析、向量化、检索、LLM 计划和 PyDESeq2 计算；剩余 Adapter 必须继续明确标识数据来源、降级行为和生产替换点，不能把确定性数据伪装成模型或外部数据库结果。

## 四 保留、改进、删除和新增决策

### 保留

- bulk RNA-seq 差异表达作为主演示场景；
- 任务状态机、鉴权、文件结构解析、SSE、失败重试、取消和刷新恢复；
- RAG 证据链、代码流、结果血缘和轻量 3D 作为前端亮点；
- 首次访问引导和中文操作手册。

### 必须改进

- 保持能力中心 `/skills` 和文件中心 `/files` 的独立页面职责；
- 保持项目/任务 URL 驱动，并继续补齐草稿任务的完整服务端运行身份；
- 把固定“39 秒”、固定任务数量和硬编码技能条目改成服务端快照；
- 对话消息增加引用、Trace、复制、重试、后续问题和运行状态；
- 首屏引导不只介绍按钮，要提供“加载示例数据 → 自动填充澄清 → 生成计划”的可执行 CTA；
- RAG 视图直接展示项目文件、技能包、文献、知识图谱四类证据的输入、输出、相关度和参数绑定；
- 3D 视图增加真实 PDB/CIF 适配器边界、结构加载状态、错误降级和低性能模式。

### 删除或降级

- 删除能力中心弹窗对技能目录的主职责，保留工作台短入口；
- 删除没有服务端来源的静态运行时间、静态“24 个样本”以及与任务状态无关的固定数字；
- 删除“点击后只发 Toast”的伪交互，替换为页面、详情或状态更新；
- 保留轻量 Canvas 3D 作为 fallback，但不要把它包装成完整 Mol\*；
- 不在两天内实现多人协作、真实 HPC 调度和所有 80+ 技能，改为技能目录可扩展结构。

### 新增

- `/projects/[projectId]`、`/projects/[projectId]/tasks/[taskId]`、`/skills`、`/files` 页面级路由；
- `SkillCatalog`、`SkillCard`、`SkillFilters`、`FileCatalog`、`TaskList` 领域组件；
- `DemoModeBanner`，显示“演示模式 / 数据来源 / 可替换服务”；
- 一键“使用示例数据开始”入口和可恢复 onboarding 状态；
- 消息级 Trace/citation 与 RAG 关系图联动；
- `prefers-reduced-motion`、粒子数量分级和动画开关；
- 路由级、API 级、浏览器级验收脚本，防止页面看起来能点但 URL/服务端没有变化。

## 五 重构后的目标用户路径

### 新用户三分钟路径

```text
进入 /projects/demo/tasks/rna-seq
  → 首次引导解释“问题 → 文件 → 证据 → 计划 → 结果”
  → 点击“使用示例数据开始”
  → 服务端导入真实 TSV/CSV 摘要
  → 自动填充四项澄清并允许用户修改
  → 生成计划并展示证据来源
  → 用户审批
  → 运行工作流，查看 RAG、代码、日志和节点状态
  → 结果、火山图、报告和 3D 结构联动
```

### 资深面试官观察路径

```text
点击左侧“中心” → /skills 独立技能目录
  → 搜索 DESeq2 / protein structure
  → 按领域筛选并查看技能详情/输入输出/启用状态
  → 回到任务页
  → 点击回答引用 → RAG 来源和知识图谱关系
  → 拖拽工作流并保存版本
  → 失败节点重试，刷新页面确认状态恢复
```

## 六 技术方案重构原则

### 页面与路由

```text
app/
  page.tsx                                      根入口，重定向或展示项目列表
  projects/[projectId]/page.tsx                项目概览与任务列表
  projects/[projectId]/tasks/[taskId]/page.tsx 任务工作台
  skills/page.tsx                              技能目录
  skills/[skillId]/page.tsx                    技能详情与启用状态
  files/page.tsx                               文件目录和结构摘要
```

路由是业务状态的一部分：刷新、分享、浏览器返回和面试官直接打开 URL 都必须可恢复。弹窗只适合短确认，不承担页面级业务。

### 服务端边界

- 页面只调用 `lib/api-client.ts`，不在组件内拼接接口；
- `ProjectService`、`TaskService`、`SkillCatalogService`、`FileProfileService`、`RunService` 分离；
- 演示模式与真实模式使用相同领域 DTO，差异通过 Adapter 注入；
- 所有写 API 继续使用 HttpOnly 会话和同源校验；
- 任务状态以服务端持久化为真相，SSE 只负责增量通知；
- 技能、文件、任务和 Artifact 均返回 `id/version/updatedAt/source/status`，前端不使用静态数量冒充服务端状态。

### 视觉与交互亮点

- 以“证据链”作为生命科学主题的主视觉：DNA 双螺旋/节点关系只做轻量动态背景，不使用无意义粒子噪声；
- 运行中的连接线显示事件传播，RAG 节点显示“检索 → 重排 → 绑定”，结果节点显示 Artifact 版本；
- 对低性能设备使用静态关系线、减少粒子数量和关闭模糊；尊重 `prefers-reduced-motion`；
- 3D 结构的亮点不是“有一个旋转球”，而是候选基因、pLDDT、残基、证据和结果指标形成可解释联动；
- 每个主操作同时提供当前状态、下一步动作和失败恢复入口，小白不需要猜下一步。

## 七 下一轮验收标准

### 页面级

- 点击左侧“中心”后 URL 进入 `/skills`，刷新仍在技能目录；
- 点击“文件”后 URL 进入 `/files`，上传后列表和结构摘要来自 API；
- 点击任务卡后 URL 带项目和任务 ID，浏览器返回/前进可用；
- 每个主按钮都有真实状态变化，不以 Toast 作为唯一结果。

### Agent 级

- 一次演示能从示例数据走到审批、运行、失败、重试、结果和刷新恢复；
- 消息中的 Trace 和 citation 能打开对应证据；
- RAG 四类来源至少各有一条可审查证据；
- 所有 Mock 明确标识来源和替换点。

### 前端工程级

- 页面组件不超过单一领域职责；
- `typecheck`、`format:check`、生产构建、API smoke、路由 smoke 和浏览器回归全部通过；
- 1440、1280、1024、768、390 五档没有横向溢出、遮挡或无法关闭的弹层；
- `prefers-reduced-motion` 下仍可读、可操作、可完成主流程。

## 八 面试时的诚实表述

可以说：

> 我先把 Biomni 的任务工作台、技能中心、文件空间和工具面板拆成可验证的页面与状态。当前版本的新建任务具有独立 URL 和服务端快照，可以从真实文档上传，经解析、FastEmbed/Qdrant 混合检索和真实 LLM 计划，进入审批与 PyDESeq2 异步计算，再查看可追溯结果。旧 RNA-seq 任务保留为稳定演示入口；OCR、知识图谱和完整 Mol\* 仍有明确降级边界。

不应说：

- “已经 100% 复刻 Biomni”；
- “RAG、DESeq2、3D 模型都已经达到生产级”；当前是本地真实检索与计算，仍缺生产调度、基准评测和完整分子渲染。
- “能力中心和文件中心已经接入企业生产数据”；当前是本地持久化目录，包含清楚标识的种子和用户上传数据。
- “OCR、知识图谱和所有科研技能都已完成”；这些仍是明确的下一阶段能力。
