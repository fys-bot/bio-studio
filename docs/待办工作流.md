# BioFlow Studio 当前待办

更新时间：2026-09-08

本文档只保留尚未完成或需要面试前再次验证的事项。已完成能力、启动方式和架构以根目录 README 为准；不再保留旧端口、旧 Mock 判断和过期百分比。

## P0：面试前必须确认

- [ ] 在 390、768、1024、1280、1440 五档完成最终浏览器截图回归。
- [ ] 逐页检查登录、项目、任务、能力、技能详情、文件、管理员页面的横向溢出和滚动容器。
- [ ] 检查所有 Dialog 的 Esc、遮罩关闭、最大高度、内部滚动和移动端全屏/底部策略。
- [ ] 走通一次真实计划 SSE，记录 Worker/供应商耗时和失败提示。
- [ ] 走通一次 PyDESeq2 SSE，从 queued 到 succeeded 并下载四个产物。
- [ ] 执行完整质量门禁：typecheck、format、parser tests、smoke、build、diff check。
- [ ] 录制 5–8 分钟演示视频；这是提交者需要完成的最终材料。

## P1：前端继续加分

- [ ] 增加仓库内 Playwright E2E 和五档视觉快照，替代纯人工截图回归。
- [ ] 为文件中心增加列表/网格视图切换和文件行更多菜单；当前核心预览与删除已完成。
- [ ] 为任务增加重命名、归档、复制链接和服务端收藏；当前收藏保存在浏览器 localStorage。
- [ ] 增加全局命令面板：搜索任务、文件、技能和操作，并支持键盘导航。
- [ ] 增加画布小地图、框选、多节点拖动、撤销/重做和键盘快捷键。
- [ ] 增加 RAG 命中片段与工作流节点、文档原文之间的双向高亮定位。
- [ ] 增加长作业后台通知和“离开页面仍运行”的更清晰状态入口。
- [ ] 完成 CSS 历史样式清理，减少 `globals.css/product.css/ui-contract.css` 的重复规则。

## P1：真实科研能力

- [ ] 配置并验收视觉 OCR 模型，覆盖扫描 PDF、图片、公式和复杂表格。
- [ ] 选择生命科学领域 embedding/reranker 基准集，对 multilingual MiniLM 与候选模型做 Recall@K、MRR、延迟和内存评测。
- [ ] 启用并验收 cross-encoder；当前真实检索为 BM25 + cosine + RRF。
- [ ] 接入真实知识图谱查询，替换当前用于解释交互的关系 Adapter。
- [ ] 将单细胞、通路富集和结构技能绑定真实计算器；当前只有 RNA-seq PyDESeq2。
- [ ] 接入完整 Mol* 或 3Dmol.js，并验证 PDB/CIF、残基选择和大结构性能。

## P2：生产化

- [ ] 按 `docs/数据库设计.sql` 将任务、用户、项目、运行、RAG 和产物迁到 PostgreSQL。
- [ ] 将原件和结果迁到 S3/MinIO/OSS，增加签名 URL、保留策略和病毒扫描。
- [ ] 将 Qdrant Local 切换为 Qdrant Server，增加 collection migration 和备份。
- [ ] 使用 Redis/RabbitMQ/Temporal 等替换 SQLite 轮询队列，补幂等、重试、可见性超时和死信队列。
- [ ] 将统计计算放入隔离 Worker，限制 CPU、内存、磁盘、网络和最长执行时间。
- [ ] 增加可撤销 Session、Refresh Token、CSP、审计日志、租户隔离、配额、监控和告警。
- [ ] 增加数据删除/导出、备份恢复和生命科学材料外发审批策略。

## 当前必须诚实说明

- 文件解析、FastEmbed、Qdrant、BM25/RRF、LLM 计划、SSE、SQLite 作业队列和 PyDESeq2 已真实接入。
- 示例 Count/metadata 是合成练习输入，计算本身是真实执行。
- OCR 未配置时返回 `needs_ocr`；知识图谱、完整 Mol* 和多数技能计算器仍是 Adapter/降级实现。
- 本地 JSON/SQLite/Qdrant Local 是零 Docker 面试交付方案，不是生产多租户架构。
