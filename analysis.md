# Biomni 逆向分析

## 1. 产品定位与目标用户

Biomni 是面向生命科学研究的 Agent 工作区。用户提交自然语言科研问题，Agent 检查项目文件、澄清实验设计、调用技能与计算资源，并持续交付可追溯的研究产物。

目标用户包括生物信息学研究员、湿实验研究人员、药物研发团队和需要复现分析过程的计算生物学专家。

## 2. 核心用户路径

```text
提出科研问题 → Agent 检查文件 → 对话式澄清 → 生成分析计划
→ 用户确认 → 工具/技能执行 → 流式状态与 Trace → 结果/文件/待办
```

Biomni 的关键不是静态聊天，而是把模糊问题转成可审查、可干预、可恢复的长任务。

## 3. 页面与交互

- 左侧：项目切换、任务列表、云盘和文件上传。
- 中央：对话时间线、工具调用、结构化澄清、计划确认、结果消息和输入框。
- 右侧：待办、结果、计算、笔记等可选工具抽屉，不应默认挤压对话区。
- 任务状态：等待输入、运行中、等待审批、失败、已完成。

## 4. 本项目取舍

### 保留

- RNA-seq 差异表达作为主场景；
- 服务端鉴权、API、文件持久化、SSE、代码流、失败重试、取消和刷新恢复；
- 证据来源、结果血缘和轻量 3D Artifact。

### 改进

- 澄清问题改为对话中的分步卡片，而不是独立后台表单；
- RAG 检索、证据重排和工具调用作为 Agent Trace 展开；
- 工作流画布改为分析计划的次级视图；
- 结果、待办、计算、笔记改为按需打开的工具抽屉；
- 中屏和移动端采用抽屉/底部入口，不保留隐藏面板占位。

### 暂不实现

- 真实 RNA-seq 计算、多人协作、生产级队列和完整 Mol* 能力；
- 以确定性 Mock 演示真实 Agent 的状态机和替换点。

## 5. 技术实现判断

- 前端：Next.js App Router、React、TypeScript、CSS 响应式布局。
- 后端：Route Handler + HttpOnly HMAC 会话 Cookie。
- 运行状态：服务端 Store 持久化任务、节点、Artifact 和事件；SSE 发送增量事件。
- 真实模型替换点：将确定性运行器替换为 Agent Adapter，保持事件协议不变。

## 6. 降级与风险

- 真实模型或计算服务不可用时，使用确定性 Mock 事件流；
- 3D Viewer 不可用时保留静态结构 Artifact；
- 当前文件 Store 适合单机 Demo，生产环境替换为 SQLite/Postgres 与任务队列；
- 不在前端、Git 或文档中保存真实 API Token。

## 7. 验收重点

- 首屏以对话和 Agent Trace 为主；
- 澄清、审批、运行、失败、重试、取消、恢复链路可演示；
- 结果文件、代码、日志和证据可以回溯到节点；
- 大屏、中屏、移动端无隐藏列空白，抽屉可通过遮罩关闭。
